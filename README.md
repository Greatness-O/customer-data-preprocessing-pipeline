# customer-data-preprocessing-pipeline
This project implements a complete customer data preprocessing workflow using Python. The raw customer dataset was transformed from a flat CSV structure into nested JSON records suitable for downstream analytics and business applications.
